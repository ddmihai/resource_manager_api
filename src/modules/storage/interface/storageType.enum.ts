export enum StorageType {
    WAREHOUSE = 'warehouse', // solids
    SILO = 'silo',           // granular solids (coal, grain)
    TANK = 'tank',           // liquids (oil, petrol)
    BATTERY = 'battery',     // electricity (kwh)
    CHEMICAL = 'chemical',   // hydrogen, chemicals
}
